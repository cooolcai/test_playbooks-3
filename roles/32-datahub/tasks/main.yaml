- name: echo user server_name output
  shell: "echo 'Currently {{ user }} is logining {{ server_name }}' >> {{ output }}"
- name: date-2
  shell: "date >> {{ output }}"
- name: input space
  shell: "echo 'datahub command done' >> {{ output }}"  
- name: input space
  shell: "echo ' ' >> {{ output }}"  